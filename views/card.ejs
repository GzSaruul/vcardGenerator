<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Employee Info</title>
  <link rel="stylesheet" href="/css/card.css"/>
  <script type="module" src="/js/card.js"></script> <!-- Fixed path -->
</head>
<body>
  <div class="container">
    <div class="back-button">
      <button onclick="window.location.href='/dashboard'">Back to Dashboard</button>
    </div>
    <h1>Employee Information</h1>

    <!-- English Info -->
    <h2>Name (English): <%= employee.Name_EN %> <%= employee.LastName_EN %></h2>
    <h3>Department: <%= employee.Department_EN %></h3>
    <p>Occupation: <%= employee.Occupation_EN %></p>
    <p>Company: <%= employee.CompanyName_EN %></p>

    <h3>Contact Info</h3>
    <p>Phone: <%= employee.Phone %></p>
    <p>Email: <%= employee.Email %></p>
    <p>Website: <%= employee.Website %></p>

    <h3>Address (English)</h3>
    <p>
      <%= employee.Street_EN %>, 
      <%= employee.City_EN %>, 
      <%= employee.Region_EN %>, 
      <%= employee.Postal_EN %>, 
      <%= employee.Country_EN %>
    </p>

    <!-- Mongolian Info -->
    <h2>Нэр (Mongolian): <%= employee.Name_MN %> <%= employee.LastName_MN %></h2>
    <h3>Хэлтэс: <%= employee.Department_MN %></h3>
    <p>Албан тушаал: <%= employee.Occupation_MN %></p>
    <p>Компани: <%= employee.CompanyName_MN %></p>

    <h3>Холбоо барих мэдээлэл</h3>
    <p>Утас: <%= employee.Phone %></p>
    <p>И-мэйл: <%= employee.Email %></p>
    <p>Вебсайт: <%= employee.Website %></p>

    <h3>Хаяг (Mongolian)</h3>
    <p>
      <%= employee.Street_MN %>, 
      <%= employee.City_MN %>, 
      <%= employee.Region_MN %>, 
      <%= employee.Postal_MN %>, 
      <%= employee.Country_MN %>
    </p>

    <div class="qr-code">
      <% if (employee.qr_data) { %>
        <img src="<%= employee.qr_data %>" alt="QR Code" id="employeeQrCode">
      <% } else { %>
        <p id="noQrCode">No QR Code Available</p>
      <% } %>
    </div>
    <a href="/download-vcard/<%= employee.id %>">Download vCard</a>

    <button id="generateQrButton">Generate QR Code</button>
  </div>
</body>
</html>
